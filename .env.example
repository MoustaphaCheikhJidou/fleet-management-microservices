# MySQL
MYSQL_ROOT_PASSWORD=ChangeMeRootPassword
MYSQL_ROOT_HOST=%
MYSQL_ALLOW_EMPTY_PASSWORD=no

# RabbitMQ
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# Gateway / JWT
JWT_SECRET=ChangeThisJWTSecret

# Build metadata
BUILD_TIME=unknown
GIT_COMMIT=unknown
# MySQL
MYSQL_ROOT_PASSWORD=ChangeThisRootPassword
MYSQL_ROOT_HOST=%
MYSQL_ALLOW_EMPTY_PASSWORD=no

# RabbitMQ
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672

# JWT
JWT_SECRET=ChangeThisJWTSecret

# Build metadata (optional)
BUILD_TIME=unknown
GIT_COMMIT=unknown
# MySQL
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_ROOT_HOST=%
MYSQL_ALLOW_EMPTY_PASSWORD=no

# Exposed host port mapping for MySQL (host:container)
# Keep default mapping in docker-compose.yml (3307:3306)

# RabbitMQ
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=admin123

# Gateway / build metadata (optional)
BUILD_TIME=unknown
GIT_COMMIT=unknown

# JWT secret used by Gateway (example only - do NOT commit secrets)
JWT_SECRET=your_jwt_secret_here
